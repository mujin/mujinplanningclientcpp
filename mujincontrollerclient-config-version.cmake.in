set( PACKAGE_VERSION "@MUJINPLANNINGCLIENT_VERSION@" )
if( "${PACKAGE_FIND_VERSION}" VERSION_EQUAL "@MUJINPLANNINGCLIENT_VERSION@")
  set(PACKAGE_VERSION_EXACT 1)
  set(PACKAGE_VERSION_COMPATIBLE 1)
endif()

if( "${PACKAGE_FIND_VERSION}" VERSION_LESS "@MUJINPLANNINGCLIENT_VERSION@")
  set(PACKAGE_VERSION_COMPATIBLE 1)
endif()
